<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render partial: 'shared/feed_item', collection: @feed_items %> <!-- with this syntax, 'render' will 
                                                                      render every element of the 'collection -->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
